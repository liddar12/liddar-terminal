# Copy to .env (gitignored) and fill in. Never commit real values.
EIA_API_KEY=your-eia-open-data-key
POWER2026_ISO=ERCOT
# GRIDSTATUS_MODE=oss        # 'oss' (free lib) or 'hosted' (needs GRIDSTATUS_API_KEY)
# GRIDSTATUS_API_KEY=...
